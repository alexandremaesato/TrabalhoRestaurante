<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.alexandre.trabalhorestaurante" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".SplashScreen"
            android:label="splashScreen"
            android:theme="@android:style/Theme.Light.NoTitleBar.Fullscreen" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="Resturant" >
        </activity>
        <activity
            android:name=".Dashboard"
            android:label="Resturant" >
        </activity>
        <activity
            android:name=".Pagamento"
            android:label="@string/title_activity_pagamento" >
        </activity>
        <activity
            android:name=".EfetuarPedido"
            android:label="Escolha de Itens" >
        </activity>
        <activity
            android:name=".ConfirmaPagamento"
            android:label="@string/title_activity_confirma_pagamento" >
        </activity>
    </application>

</manifest>
